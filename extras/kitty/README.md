# Kitty luna Theme

1. Copy `luna.conf` into your Kitty config directory (usually `$HOME/.config/kitty`).
2. Include it from `kitty.conf` with `include luna.conf`.
3. Reload Kitty with `CTRL+SHIFT+F5` to apply the change.
