# Yazi luna Theme

1. Navigate to your yazi config directory (usually `$HOME/.config/yazi`).
2. Copy `luna.toml` there as `theme.toml`, or merge its contents into your existing `theme.toml`.
3. Restart yazi to apply the change.
